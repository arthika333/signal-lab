# Signal Lab

An LLM-augmented news-to-signal research pipeline. It ingests market data and financial news, uses an LLM to extract structured judgments from unstructured headlines, combines those with technical indicators into a trading signal, and
backtests the result **without lookahead bias and with transaction costs**.
